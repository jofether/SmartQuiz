# Dependencies required by the Lambda deployment package.
# Package them with the function code or publish via a Lambda layer.

boto3>=1.33.0
PyPDF2>=3.0.0
firebase-admin>=6.0.0
google-cloud-firestore>=2.13.0
google-generativeai>=0.4.0
